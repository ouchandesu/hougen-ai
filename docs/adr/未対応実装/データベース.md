# データベース（DB-）

Supabase 上のテーブル（`profiles` / `usage_logs` / `training_records` / `dialect_entries` / `broadcast_examples`）と RPC（`get_admin_stats`）。

| ID | 種別 | 内容 | 根拠 |
| --- | --- | --- | --- |
| DB-164 | 負債 | テーブル・RLS ポリシー・`get_admin_stats` の定義がリポジトリに無く、コードが前提にする列名や戻り値と照合できない | `api/_log.js`, `api/admin.js`, `admin.html` |
| DB-372 | 未検証 | `dialect_entries` / `broadcast_examples` の SQL（テーブル・CHECK 制約・RLS）が Supabase に適用済みか、anon key で下書きの語が読めないか、管理 API の upsert と用例の追加が実 DB で通るかを確かめていない（手元では疑似 Supabase でのみ確認） | `api/_dictionary.js`, `api/dictionary.js` |
| DB-538 | 未検証 | 学習記録の読み出しが `user_id` で絞らずに `training_records` を select しており、他人の行が見えないことは RLS だけに依存している。RLS の中身をリポジトリから確かめられない | `index.html`（`loadRecords`） |
